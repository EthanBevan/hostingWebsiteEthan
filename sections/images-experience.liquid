<div id="section1_e_experience">
  <div class="image_card_e_experience">

    <h3> {{ section.settings.text-box }} </h3>

{% for block in section.blocks %}

<div class="gallery_e_experience">
  <a href="{{ block.settings.link }}">
    <img class="image_e_experience" src="{{ block.settings.image | img_url: 'x800' }}">

 <!--    <p class="text_e_experience">
            Hello World!
       </p> -->
  </a>
</div>

{% endfor %}

  </div>

</div>

<div class="padding_e_experience">
  <h3>h</h3>
</div>

{% schema %}
{
      "name": "Image Experience",
      "settings": [
      {
        "id": "text-box",
        "type": "text",
        "label": "Heading",
        "default": "Title"
      }
    ],
      "blocks": [
      {
        "type": "image_picker",
        "name": "Add Image",
        "settings": [
          {
            "id": "link",
            "type": "url",
            "label": "Button link"
            },
            {
              "id": "image",
              "type": "image_picker",
              "label": "Image Upload"
            }
          ]
        }
      ],
      "presets": [
      {
        "name": "Image Experience",
        "category": "Call to Action",
        "blocks": [
        {
          "type": "image_picker"
        },
        {
          "type": "image_picker"
    }
  ]
}
]
}
{% endschema %}

<!-- Add in blocks to schema
Add for loop around images -->
